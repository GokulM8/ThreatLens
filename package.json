{
  "name": "threatlens",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --names \"FRONTEND,BACKEND\" --prefix-colors \"cyan,green\" \"npm run dev --prefix frontend\" \"backend/.venv/bin/python -m uvicorn backend.main:app --reload\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "backend/.venv/bin/python -m uvicorn backend.main:app --reload",
    "install:all": "npm install && cd frontend && npm install",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
