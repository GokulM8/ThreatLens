# Optional — backend runs fully offline (local JSON registry, in-memory
# dashboard) without these. Set them to use real Supabase persistence.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
